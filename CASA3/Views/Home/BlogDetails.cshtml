@model Core.DTOs.BlogPostDto

@{
    ViewData["Title"] = Model?.Title ?? "Blog Details";
}

<section class="bg-white py-12">
    <div class="max-w-4xl mx-auto px-6">
        <a href="@Url.Action("Blog", "Home")" class="text-sm text-gray-600 hover:text-gray-800">← Back to Blog</a>

        <h1 class="text-4xl font-bold mt-6 mb-2" style="color: #4A4A4A;">@Model.Title</h1>
        <p class="text-sm text-gray-500 mb-6">@Model.PublishedDate.ToString("MMMM dd, yyyy") &middot; <span class="font-semibold">@Model.Author</span> &middot; <span style="color:#b39359;">@Model.Category</span></p>

        @if (!string.IsNullOrEmpty(Model.CoverImageUrl))
        {
            <div class="mb-6 overflow-hidden rounded-lg shadow-md">
                <img src="@Model.CoverImageUrl" alt="@Model.Title" class="w-full h-auto object-cover" onerror="this.src='/images/projects/project-1.webp'" />
            </div>
        }

        <div class="prose max-w-none text-gray-700 mb-8">
            <p>@Model.Content</p>
            <p>@Model.Content</p>
            <p>@Model.Content</p>
        </div>

        <div class="mt-8">
            <span class="text-sm text-gray-500">Views: @Model.Views</span>
        </div>
    </div>
</section>